# THEOREM_S3, Addendum 3 (2026-09-08): M7 discharged, M3 enclosed and shown to be M5, and both of `fix2`'s new open items closed

Source: `round2/THEOREM_S3/fix3/FIX3.md` and its scripts (`g1_vstrain.py`, `g2_theta_tail.py`,
`g3_jmismatch.py`, `g4_transported.py`, `g5_propagate.py`; `SHA256SUMS` computed with
`shasum -a 256`). `THEOREM_S3.md`, `ADDENDUM_1`, `ADDENDUM_2` and every other existing file are
unchanged; everything new is in `fix3/`.

---

## 1. THE UPDATED MODULO LIST

| item | status after this addendum |
|---|---|
| **M1** (P-max) | **DISCHARGED**, ADDENDUM_1 (`pmax-h3v` Part A). Unchanged. |
| **M2** (H3-V) | **DISCHARGED**, ADDENDUM_1, as Theorem V.4′ with `(H3′_ϑ)`, `f ≥ 4`. Unchanged. |
| **M3** (V-strain) | **ENCLOSED, and it is M5.** The reduction is proved: the strain kernel is `𝒦 = ∂_zG₅ = −3z/(8π²ρ⁵)`, its weight is **exactly `3/8` per e-fold**, the difference `w = η − η^tr` obeys `D_tw = νΔ₅η` with `w(0) = 0` and therefore `sup ρ\|w(s)\| ≤ νe^{c_R}∫₀^sΨ`, and `ε_a = (3/8)(1 + 1/(4L))Ψ̄ c e^{c_R}/(s²L)`, the `O(c/(s²L))` the item expects. The one input left is `Ψ̄ = sup ρ\|Δ₅η\|`, and for THEOREM_S3's **kinked** angular profile `Ψ(0) = +∞`: `Δ₅η₀` carries a Dirac mass on each of the four kink cones. Mollifying the angle at scale `σ` makes it `Ψ_σ(0) = 8.07/σ`. **M3 and M5 are one item.** |
| **M4** (λ-cap) | unchanged; a slack, not a gap. |
| **M5** (H5-Lip) | unchanged as stated, and now **load-bearing for M3 as well**. The `σ`-mollification that closes it is *not* the trade THEOREM_S3 §3 describes: at `σ = 0.05` rad it lowers `𝔊₀` from `58.695` to `25.289` **and** raises `κ_δ` from `0.4978224` to `0.4983883`. Both moves are favourable. (D-C)'s bad `κ` comes from widening the equatorial layer, not from rounding the corners. |
| **M6** (H\*) | unchanged. |
| **M7** (Θ-tail) | **DISCHARGED.** `hk2`'s (D1) is replaced by **(D1′)**: `\|ω^θ(·,t)\| ≤ M_t ≤ (3/2)M` on `ℝ⁵`, and the shell density (2.1) holds at **every** `ρ′ ∈ (0,∞)` with one finite `𝒥`. No support statement is used anywhere in `hk2` §§4–5: Lemma 5.2's four integrals already run over `(2ρ,∞)` and `(0,ρ/2)`, and `k3.lambda_bulk` integrates the actual tailed field. The tail corrections are tabulated in §3 and are `≤ 1.7e-04` relative or exactly zero. |
| **M8** (`L_Γ^exist`) | unchanged (renamed by ADDENDUM_2 item 5). |
| *new, from ADDENDUM_2*: the `𝒥` mismatch | **SETTLED.** `96.25` is a supremum over `ρ′`; `65.63` and `78.64` are plateau values of two other data. Like for like: supremum `96.26` against (D-B)'s **`148.83`** (the theorem's datum is `1.55×` **better**); plateau `75.26` against `65.63` (worse by `1.15×`, not `1.47×`). `𝒥` is read by no script that produces a constant, so `K₂`, `C″` and `L_*` are unchanged. |
| *new, from ADDENDUM_2*: transported bound vs untransported measurement | **REFUTED.** `hk2` §9's measured column is the **transported** field (its own parenthesis: "measurement by `k9` on the transported field"); what `fix2` read was `k4_results.json`'s withdrawn block. A third instrument reproduces `2.190342 / 3.382506 / 5.385261` at `λ = 1, 1.25, 3/2` against `hk2`'s `2.1903 / 3.3827 / 5.3854`. The `30.4×` slack stands. |
| *carried* | `refute-u2` MAJOR-1 (the P1/P2 "sharpness" tautology) and MAJOR-2 (the datum choice) stand. ADDENDUM_2's five repairs and the withdrawal of the `ε ≤ 1/2` column stand. |
| *new, owed to the budget* | `t3_budget.py:65` sets `ELL_RAMP = 0.25`; the datum's exact e-fold content is `∫Θ du = L − 2ε_r = L − 0.5`, so the ramp costs `0.5` and the missing `0.25` enters `ε_a` as `0.25/L` (`2.5e-07` at `L = 10⁶`). Conservative to fix, below every other term. |

---

## 2. THE `L_*` TABLE

Nothing in this addendum moves `L_*`. The table is ADDENDUM_2's, with the proved column
**re-run here** from a byte copy of `fix2/f1_window.py` as an L-11 control, and with the two
new terms priced against it.

`log Λ_* = 2L_* + 2.9234859`. Self-consistent window, `f ≥ 4` (ADDENDUM_1), proved column
re-run in `fix3/g3_jmismatch.py` §D:

```
  column                    L_*(eps<=0.1943662)  log Lambda_*    L_*(eps<=0.1)     log Lambda_*
  all proved                    1887786.7127      3775576.3488    1977309.3012      3954621.5258
  proved, C_a measured          1848687.4         3697377.6       1932180.6         3864364.0
  all measured                     1538.37           3079.67         1701.58           3406.09
```

The proved row is re-run here; `1887786.7127` and `1977309.3012` reproduce ADDENDUM_2's
`1887786.7` and `1977309.3`. The other two rows are quoted from ADDENDUM_2 unchanged.

**Sensitivity of that row to everything this addendum touches:**

| perturbation | `L_*` (`ε ≤ 0.1943662`) | relative move |
|---|---|---|
| `C_K = 161.7735` (the budget's import), the row above | `1887786.7127` | n/a |
| `C_K = 33.710` (`fix2`'s quadrature `K̂₂`, S3 datum, `λ_max`, `f = 4`) | `1887786.6179` | `−5.0e−08` |
| `C_K = 102.6625` (the fully analytic `Λ` route at `𝒥 = 96.26`) | `1887786.7127` | `0` |
| `C_K = 176.4010` (the same route with `𝒥` **doubled**) | `1887786.7127` | `0` |
| `ε_a` added to `ε_a`'s budget line, `σ = 0.002` mollification, `L = 10⁶` | n/a | `ε_a = 7.30e−05`, i.e. `0.069 %` of `ε(10⁶) = 0.106218` |
| `ε_a` on the kinked `√` route, `L = 10⁶` | n/a | `ε_a = 3.44e−03`, i.e. `3.24 %` of `ε(10⁶)` |
| `ELL_RAMP 0.25 → 0.5` | n/a | `+0.25/L = 2.5e−07` at `L = 10⁶` |
| the Θ-tails, on `Λ₄`, `Λ₅` | n/a | `≤ 1.7e−04` relative, and `hk2`'s quadrature already includes them |

`ε(10⁷, proved) = 0.014141132567409676` is identical to sixteen digits at all four `C_K`.

---

## 3. THE Θ-TAIL CONSTANTS, FOR THE RECORD

Exact, with `ε_r = 1/4` and `ψ_in = e²/(e² + (ρ/ρ₀)⁸)`:

```
    int_{-inf}^{inf} Theta du  =  L - 2 eps_r  =  L - 0.5          (EXACT)
    int_{-inf}^{0}   Theta du  =  log(1 + e^{-2})/8  =  0.0158660  (EXACT, each end)
    Theta  <=  e^{-2} (rho/rho_0)^8   for rho < rho_0 ,   <=  e^{-2}(R/rho)^8  for rho > R  (EXACT)
    J(rho') <= Theta(rho') * 236.512  (inner tail) ,  <= Theta(rho') * 292.662  (outer tail)
```

so the tail suppression is exactly the `C e^{−2}` the item expects, with `e^{−2}` the `e²` of
`ψ_in` and `1/8 = ε_r/2`. Measured (`L = 10` and `L = 40`, identical):

| quantity | value |
|---|---|
| `𝒥` supremum over `ρ′` (what (2.1) asks) | `96.2602`, at `u = L − 0.336`, in the **outer ramp** |
| `𝒥` plateau (`Θ ≡ 1`) | `75.2511` |
| `𝒥` supremum over the whole inner tail `u < 0` | `25.0233` |
| `𝒥` supremum over the whole outer tail `u > L` | `31.6880` |
| `𝒥(u = −1)` / `𝒥(u = L+1)` | `0.010737` / `0.013286` |
| `Λ₄` full vs shell-masked, `λ = 1`, `f = 4` | `26.574429` vs `26.570094` (tails `1.63e−04`) |
| `Λ₅` full vs shell-masked, `λ = 1`, `f = 4` | `7.192621` vs `7.191770` (tails `1.18e−04`) |
| `Λ₄`, `Λ₅` at `λ = λ_max` | tails `1.04e−06`, `3.46e−07` |
| far/near constants `C1 = 0.291999`, `C2in = 0.014754`, `C_collar = 3.999218/sin φ + π/8` | unchanged (their hypotheses are pointwise bounds on `\|ω^θ\|`, true on all of `ℝ⁵`) |

**The tails do not raise `𝒥`.** The supremum lives in the outer ramp, `3.85×` above the
inner-tail supremum and `3.04×` above the outer-tail one.

---

## 4. THE ONE THING TO DO NEXT

M3 is now a corollary of M5, and M5 has a cheap closure that no earlier sheet priced: mollify
the angular profile at a fixed `σ`. At `σ = 0.002` rad the cost is `Ψ_σ(0) = 4130`, giving
`ε_a = 7.3e−05` at `L = 10⁶`, `0.069 %` of the budget, and `κ_δ` and `𝔊₀` both move the right
way (`0.4978224 → 0.4978233`, `58.6946 → 54.2132`). At `σ = 0.05` rad the gain is much larger
(`𝔊₀ = 25.29`, `κ_δ = 0.4983883`, `ε_a = 4.3e−06`) but the datum has genuinely changed and
`c_*`, `r_h`, `P_h`'s monotonicity and `C_E` would all have to be recomputed for it. Deciding
which `σ`, or none, is Alexander's; this addendum only puts the trade on one table.

**FL-000 stands. Nothing here touches the headline problem.**
