# THEOREM_S3 — Addendum 2 (2026-09-08): the five certification defects repaired, the window made self-consistent, and the `ε ≤ 1/2` column withdrawn

Source: `round2/THEOREM_S3/fix2/FIX2.md` and its scripts (`f1_window.py`, `f2_datum_s3.py`,
`f2_ramp_k2.py`, `f2b_measured.py`, `f4_scans.py`, `f5_gamma_exist.py`; `SHA256SUMS` computed
with `shasum -a 256`). Prompted by the scope review at
`forced-route-2026-09/docs/claude-scope-review.md` §1 "Additional issues beyond exactly eight
items" and §2's last paragraph. `THEOREM_S3.md` and every other existing file are unchanged;
everything new is in `fix2/`.

## What was fixed

- **Item 1, time-window coverage: FIXED, and it is expensive.** The bootstrap estimates were
  established on `[0, τ]`, `τ = c_*/(ML)`, and the conclusion drawn at `t_* = τ(1+ε) > τ`. The
  window is now made self-consistent, `c ≥ c_*(1 + ε(c,L))`, with `λ_max = e^{3c/4}`, `r_h`,
  `ell_loss`, the majorant ODE, `c_G` and the whole viscous budget re-read at that `c`.
- **Items 2 and 3, the ramp slope and the `λ` range: FIXED.** `ρΘ′(ρ) = dΘ/du` exactly; its
  maximum is `1/(2ε_r) = 2` at `ρ = ρ₀e^{ε_r} = 1.2840254ρ₀`, and
  `ρ₀Θ′(ρ₀) → 2 sech²(1) = 0.83994868`, not `2`. §1.1's claim that this "agrees with `hk2` (D-B)
  to the digit" compared the maximum of one profile with the point value of the other and is
  void. `K₂` was then recomputed for THEOREM_S3's own datum with `hk2`'s own instrument, over
  the full `λ` range.
- **Item 4, the three discrete scans: FIXED.** `P_h(λ)/λ` is the expectation of the angular
  profile under an explicit probability measure `μ_λ` on `[0,1]`; `P_1(λ) = λ` is now a theorem;
  `r_h` is enclosed with a proved Lipschitz certificate; `P_h` increasing is proved; `E₀` and
  `𝔊₀` have closed forms.
- **Item 5, `L_Γ*`: FIXED (renamed).** `14260.5` is the Picard-convergence threshold. The
  existence boundary, from the sign change of `F(Γ̄) − Γ̄`, is `L_Γ^exist = 14259.948`.
- **Item 6, the `theorems/02` localisation slip: FIXED in text.** A smooth cutoff `χ_R` with
  its `W^{4,1}` seminorm control replaces the sharp-ball indicator; the external file is not
  edited (`FIX2.md` §6).

## The corrected theorem statement

Everything in THEOREM_S3 §1.3 stands, with five changes.

> **THEOREM (S3), assembled form, corrected.** Let `u_0` be the datum of §1.1 with
> `f ≥ 4` (ADDENDUM_1) and let `u` be its `H^s` strong solution. Assume the eight items of §3
> **on `[0, c/(ML)]`**, where `c` is the window fixed below rather than `c_*`; and assume
> `L ≥ L_Γ^exist` at that window (item 5). Then for every `L ≥ L_*` there is
> `c ∈ [c_*, 1.194366 c_*]` satisfying `c ≥ c_*(1 + ε(c,L))`, and with
> `ε(L) := c/c_* − 1` for the smallest such `c`,
> ```
>     T_d(u_0)  <=  t_*  :=  c/(M L)  =  c_* (1 + eps(L))/(M L) ,
> ```
> with `c_* = 0.8144774`, `2log(3/2)/κ_δ = c_2 = 1.6289547`, and `ε(L)` decreasing to
> `4.3822e-03` (against the frozen window's floor `(1−2κ_δ)/(2κ_δ) = 4.3741737e-03`).
> The hypotheses now hold on exactly the interval `[0, t_*]` the conclusion is about.

1. **Window.** The estimates and the conclusion now live on the same interval,
   `θ ∈ [0, c]`, `c = c_*(1+ε)`.
2. **`λ` range.** `λ ≤ λ_max = e^{3c/4}`, which at the corrected window is up to `2.0742`, not
   `1.8420112`. The `K₂` and `r_h` inputs are supplied on that range (below).
3. **`ε ≤ 1/2` is withdrawn.** A2/C6 need `P_h` increasing on `[1, λ_max]`; `P_h′` vanishes at
   `λ_mono = 2.0769162` and is negative beyond, so the window may not exceed
   `c/c_* = 1.1964878` (certified `1.1943662`), i.e. **`ε ≤ 0.194366`**. The `ε ≤ 1/2` column
   would need `λ_max = 2.499991`, where `P_h′ = −0.474818`: that column is not merely
   re-priced, it has no hypothesis.
4. **`K₂`.** For THEOREM_S3's own datum, on `N_τ`, `φ₀ = 30°`, `f ≥ 4`:
   `K̂₂ ≤ 19.169` at `λ = 1`, `25.418` at `λ = 3/2`, `33.710` at `λ = 1.8420112`, `42.008` at
   `λ = 2.0741`. The budget's imported `161.7735` (which `hk2` proved for *its* datum on
   `[1, 3/2]` at `f = 0`) is a valid majorant over the whole corrected range, by `3.851×`.
   Measured at `λ = 1`, `f = 4`: `K̂₂ = 0.29270`, slack `65.5×`.
5. **`r_h` and `𝔊₀`.** `r_h ∈ [0.9186364112, 0.9186365334]` on `[1, e^{3c_*/4}]` (ENCLOSED,
   argmin at `λ_max`), `0.8661941` at the window `c = 1.136 c_*` where `L_*` sits;
   `E₀ = 1/sin δ = 7.66129757554039` and `𝔊₀ = 1/sin²δ = 58.69548054098106`, both PROVED
   (THEOREM_S3's grid `𝔊₀ = 58.695476` was `4.54e-06` **below** the true essential supremum).

## The new `L_*` table

`log Λ_* = 2L_* + 2.9234859` (the `C_E` shift is unchanged: it is a property of the datum, not
of the window). Self-consistent window, `f` free:

```
  column                    L_*(eps<=0.194366)   log Lambda_*     L_*(eps<=0.1)    log Lambda_*
  all proved                    1887775.3         3775553.4         1977293.7        3954590.4
  proved, C_a measured          1848675.9         3697354.6         1932164.9        3864332.8
  all measured                     1532.97           3068.87           1691.84          3386.61

  THEOREM_S3 (frozen window):   eps<=1/2  311511.9 / 302704.3 / 422.4
                                eps<=0.1 1056242.9 / 1025043.0 / 1119.3
```

With ADDENDUM_1's `f ≥ 4`: `1887786.7 / 1848687.4 / 1538.37` at `ε ≤ 0.194366` and
`1977309.3 / 1932180.6 / 1701.58` at `ε ≤ 0.1`; `log Λ_* = 3775576.3` on the proved headline.

**The headline moves `311511.9 → 1887786.7`, a factor `6.060`**, and `log Λ_*` from `623026.8`
to `3775576.3` by the same factor. The whole loss is one term: at `L_*` the self-consistent
window is `c = 1.1360550 c_*`, `λ_max = 2.0016429`, and against the frozen window at the same
`L` this raises `C″` by `1.365`, `C_R` by `1.386`, `μ` by `2.235` and lowers `r_h` by `5.7 %`,
for a factor `2.433` on `ε_T'` — the binding term in every column. It is a drive-side factor,
not an exponent, which is why it costs `6` and not `e^{...}`.

## What remains

- THEOREM_S3 §3's modulo list is **untouched**: M1 (P-max), M3 (V-strain), M4 (λ-cap),
  M5 second half, M6 (H\*), M7 (Θ-tail) stand exactly as written; M2 (H3-V) and the P-max half
  of M5 were discharged in ADDENDUM_1; M8 is renamed by item 5 above.
- `refute-u2` MAJOR-1 (the P1/P2 "sharpness" tautology, `~14×` of `C″` available but unproved)
  and MAJOR-2 (the datum choice) stand.
- New and open: THEOREM_S3's datum has shell total-variation density `𝒥 = 96.25` against
  `hk2`'s `65.63` for (D-B) and `78.64` for (D-A) — `1.47×` and `1.22×`. `𝒥` is the quantity
  through which the datum swap actually propagates into `K₂`, and nobody had tracked it. It
  costs nothing here because `K₂` is `3.85×` inside its imported majorant, but it is the number
  to watch if the `K₂` margin is ever spent. The instrument reproduces `hk2`'s (D-A) and (D-B)
  rows to `4.5e-07` and `1.2e-05`; while checking it, `hk2` §8(a)'s "bare plateau, no taper"
  reference row `𝒥_ac = 45.313074` turns out not to be the quantity its own Lemma 5.1 defines
  (the Lemma's integrand gives `4π² = 39.478418`). A reference row, used in no bound.
- Also open: `hk2`'s quoted measured slack over the window compares a *transported* bound with
  an *untransported* measurement (`hk2` §14 item 7 declares the untransported evaluation wrong
  for the bound but the measured column still uses it). A measurement of the transported field
  at `λ > 1` needs an instrument neither seat has: `η_λ = η₀ ∘ T_λ^{-1}` is not separable.

**FL-000 stands. Nothing here touches the headline problem.**
