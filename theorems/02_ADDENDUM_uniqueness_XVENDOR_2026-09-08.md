# Second-vendor blind check of 02_ADDENDUM_uniqueness (capture block, L-94)

Socket: codex-cli via supercli ask, 2026-09-08 ~19:05Z; prompt = the three-item attack brief + the addendum verbatim (43,065 bytes); reply 4,557 chars, 120.5 s, deposit sha 8e1b61b8cf. Verbatim reply follows; nothing edited.

```
(1) CONFIRMED. Finite dissipation follows from the equation. Here is an independent proof using nested balls, without the note’s weighted pressure commutator.

Write M = sup_t ‖v(t)‖₂ and F = ∫₀ᵀ ‖f(t)‖₂ dt, initially taking div f = 0. The pressure gradient is necessarily that of p₀ = R_iR_j(v_i v_j): the equation makes its difference from this gradient, after testing in time, a harmonic distribution in H⁻³(R³). Such a distribution is zero, since its Fourier transform is a weighted L² function supported at the origin. Thus an arbitrary smooth pressure supplies no additional harmonic driving term.

Set D(r) = ∫₀ᵀ∫_{B_r} |∇v|². Fix R ≥ 1 and R ≤ r < s ≤ 2R, with δ = s − r. Test the local energy identity with a cutoff equal to one on B_r and supported in B_{(r+s)/2}, with derivatives bounded by Cδ⁻¹ and Cδ⁻². Split p₀ into the Riesz pressure generated by v⊗v inside B_s and that generated outside B_s. Calderón–Zygmund boundedness gives ‖p_near‖_{3/2} ≤ C‖v‖_{L³(B_s)}². On the cutoff support, the kernel bound gives |p_far| ≤ Cδ⁻³M². Consequently, the transport and near-pressure fluxes are bounded by Cδ⁻¹‖v‖_{L³(B_s)}³; the far-pressure flux is bounded by C R^{3/2}δ⁻⁴M³.

The ball interpolation inequality is ‖v‖_{L³(B_s)}³ ≤ C M^{3/2}(‖∇v‖_{L²(B_s)} + s⁻¹M)^{3/2}. Young’s inequality, followed by time integration, therefore gives, for any fixed θ > 0,
νD(r) ≤ θνD(s) + A + C_{θ,ν,M}T[δ⁻⁴ + δ⁻² + R^{3/2}δ⁻⁴],
where A = ½‖v(0)‖₂² + MF. No global gradient integrability has been used.

Iterate with r_n = 2R − R2⁻ⁿ and θ < 1/16. The error series converges because θ2⁴ < 1. The remaining term θⁿD(r_n) tends to zero because smoothness on the compact cylinder B_{2R} × [0,T] gives D(2R) < ∞. Hence νD(R) ≤ C A + C_{ν,M}T(R⁻⁴ + R⁻² + R⁻⁵ᐟ²), uniformly for R ≥ 1. Letting R increase proves finite dissipation.

Now interpolation gives v ∈ L^{10/3} in spacetime and p₀ ∈ L^{5/3}. Also v ∈ L³ ∩ L^{5/2}, so both |v|³ and |p₀v| are globally integrable on the finite slab. The ordinary cutoff energy identity therefore passes to the limit with every boundary flux vanishing. In fact, the sharper conclusion holds: ½‖v(T)‖₂² + ν∫₀ᵀ‖∇v‖₂² dt = ½‖v(0)‖₂² + ∫₀ᵀ∫f·v. The proposition’s additive constant is unnecessary.

Smooth finite-energy fields can have infinite enstrophy: place disjoint rescaled divergence-free bumps with squared L² norms 2⁻²ⁿ and spatial frequencies 2ⁿ. Their total energy is finite and their total enstrophy infinite. The decisive distinction is the nested-ball energy estimate above: the Navier–Stokes equation with the stated force excludes infinite accumulated dissipation. For general rapidly decaying f, use its Leray projection and absorb the gradient part into the pressure; only its L¹_tL²_x bound is needed here.

(2) CONFIRMED. The modified term satisfies exactly
|½∫|w|²u·∇χ_R| ≤ C R⁻¹‖u‖∞‖w‖₂².
There is no missing annulus-volume factor: the integral of |w|² is already bounded by its global L² norm squared. Uniform boundedness of the reference and competitor makes this an O(R⁻¹) inhomogeneity, independent of the localized dissipation.

After the other fluxes are absorbed, E_R′ ≤ 2‖∇u‖∞E_R + C_T/R, with E_R(0) = 0. Thus E_R(t) ≤ (C_TT/R) exp(2∫₀ᵀ‖∇u‖∞dt), and letting R increase gives w = 0. The decisive line is the global L² bound on w; no decay or annular smallness is required.

(3) CONFIRMED. Suitability really is available. Finite dissipation supplies the energy-space integrability, interpolation supplies v ∈ L^{10/3}, and pressure recovery supplies p₀ ∈ L^{5/3}. Smoothness gives the local energy equality for every compactly supported test function. Replacing P by p₀ changes the pressure by a spatial constant, whose contribution vanishes by div v = 0. Thus the local energy inequality is established, rather than assumed.

The escape argument correctly uses a fixed positive cylinder radius. All relevant translated-cylinder integrals become uniformly small at spatial infinity. In particular, the less familiar pressure quantity satisfies
∫_I(∫_B|p₀|)^{5/4}dt ≤ |B|^{1/2}|I|^{1/4}(∫_{B×I}|p₀|^{5/3})^{3/4}.
This closes that tail estimate. The stated scaling factors, including ρ⁻¹³ᐟ⁴ for this quantity, are correct. Epsilon-regularity then gives a uniform exterior velocity bound; smoothness on the remaining compact cylinder gives the interior bound. Continuity handles the top time slice.

The mathematical application of the stated forced epsilon-regularity criterion is sound. Its attribution to CKN 1982 was not independently verified here: source retrieval failed, and no browser was available.
────────────────────────────────────────────────────────────────────────
```

Verdict as read by the estate: (1) Prop D CONFIRMED with an independent nested-ball proof yielding the energy EQUALITY (no additive constant); (2) Lemma U's modified transport-flux step CONFIRMED at order R^{-1}, Gronwall closes with the global L² bound on w; (3) suitability of a Clay-class solution CONFIRMED once finite dissipation holds (v ∈ L^{10/3}, p₀ ∈ L^{5/3}, local energy equality) and the escape argument CONFIRMED; residue = the attribution of the forced ε-regularity criterion to CKN 1982, not verified at source by either line. Status of 02′: two-engine confirmed modulo that citation check.
