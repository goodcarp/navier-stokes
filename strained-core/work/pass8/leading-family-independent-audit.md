# Independent audit: the new leading family passes three actual initial tests

Accepted: the two new certificates apply to the same explicitly retuned,
smooth compact initial datum
\[
 u_{0,\lambda}=M+A_\lambda v+\lambda w_{\rm lead},\qquad
 A_\lambda^2C_v=204/35-\lambda^2C_{\rm lead},\qquad
 1/8\le\lambda\le1/4,\quad \nu=1/1000.
\]
Here
\[
 w_{\rm lead}=\operatorname{curl}
 \left[\chi\!\left(\frac{(r-7/50)^2}{(1/10)^2}\right)
 q_s(z)\cos(4\theta-20r)e_z\right].
\]
They prove, uniformly across this family,
\[
 \boxed{\partial_t\overline\Gamma(0,r_*,\pm4)>
        \frac{8871}{800}>11,}
\]
\[
 \boxed{\frac{K'(0)}{K(0)}>
        \frac{12853}{2205}>5.82,}
\]
\[
 \boxed{T=p_{zz}'(0)+32<
       -\frac{12493177}{1120000}<-11.15.}
\]
In particular the existing neutral-center identity gives
\(\beta''(0)>12493177/2240000>5.57\).
These are initial derivatives for a separate new candidate. The new
profile is not the inherited positive-time state of the previous packet.

## 1. Geometry, the declining envelope, and the shared pressure retuning

The support satisfies \(.04\le r\le.24\) and \(3.4\le|z|\le4.6\).
It avoids the axis/core, lies in the actual affine pump plateau
\(5/2<|x|<5\), and has \(r/|z|\le6/85<1/4\).
The field remains odd and fourfold-equivariant, solenoidal, and
azimuthally mean zero. All established separated-core and cone kernel
identities therefore apply.

For any radial envelope, angular averaging gives the exact formulas
\[
 R_{r\theta}=-\frac{\lambda^2mk}{2r}e^2q_s^2,\qquad
 \mathcal T=\frac{\lambda^2mk}{2r}q_s^2(e^2+2re e').
\]
The phase/envelope cross term averages to zero in the covariance,
but the derivative of the envelope must remain in the torque.
Here \(mk=-80\), so the covariance is positive. The declining envelope
at the already certified mean-maximizing radius makes the bracket
negative. The outward unit-torque interval has lower endpoint above
1950; it is not a flat-envelope substitution.

The mean still has the same unit radial function
\(g(r)=r^2\chi(r^2/a^2)\); its existing unique-root enclosure and
\(g''(r_*)>-19\) remain applicable. The axial seed cutoff is flat
on the old outer mean maximum plateau. Thus
\[
 G_t(0,r_*,\pm4)=\lambda^2\mathcal T_{\rm unit}
                       +\nu A_\lambda g''(r_*)
 >1950/64-(19/1000)1020.
\]

The radial energy estimate is correct:
\[
 R_0=\int r[(e')^2+(400+16/r^2)e^2]\,dr\le392/5.
\]
Its three parts use total variation two and \(\|e'\|_\infty<80\),
the exact support endpoints, and \(\log6<9/5\).
With \(Z_0=\int q_s^2\le12/5\), the positive transverse pressure
kernel gives
\[
 0<C_{\rm lead}\le3(5/17)^5R_0Z_0
 \le1764000/1419857<3/2 .
\]
The old \(C_v\) endpoints and \(\lambda^2\le1/16\) then imply
\(900<A_\lambda<1020\). The square root in the named exact retuning
is positive. Nonzero angular-frequency selection cancels all
base/seed bilinear pressure terms at the center, so \(p_{zz}(0)=-8\)
holds for precisely the same datum used in the other calculations.

## 2. Full initial fluctuation-energy budget

The exact integrated actual NS identity is
\[
 \frac{K'(0)}{\pi\lambda^2}
 =80A_\lambda I_rI_z-cR_0Z_0
   -\nu(R_1Z_0+R_0Z_1),
\]
where \(I_r=-\int r C_0'e^2>0\) and
\(I_z=\int q_s^2q_v\).
It follows from the earlier general covariance production
\(-\int R_{r\theta}(V_r-V/r)\), with \(mk<0\) and \(C_0'\le0\).
Pressure work and fluctuation self-advection vanish only after the
complete solenoidal spatial integration. No pointwise pressure term
has been discarded in a local evolution equation.

The \(R_1\) integrand in the script is the complete horizontal
Laplacian-square formula for this potential. Changing \(k\) to \(-k\)
leaves its two squared pieces unchanged. The axial dissipation term
\(R_0Z_1\) remains present. The range endpoints prove
\(R_1<200000\) and \(I_r>1/50\).
The two common axial cores have total length \(9/10\), so \(I_z\ge9/10\).
The established axial derivative/variation estimate gives
\(Z_1\le160/3\). These inputs reproduce exactly
\[
 K'(0)/(\pi\lambda^2)>205648/375,\qquad
 K(0)\le\pi\lambda^2 R_0Z_0/2,
\]
and hence the stated fractional gain. It is extraction from the mean;
it does not conflict with dissipation of total kinetic energy.

## 3. The full strong-swirl cross pressure is bounded, not omitted

Retain the exact Pass7 decomposition \(E=E_{\rm loc}+E_{\rm press}\).
The first pressure placement pairs to zero with the divergence-free
axisymmetric azimuthal field \(Qv\). The other two remain.
For \(h=(w\cdot\nabla)v\), incompressibility gives
\[
 \operatorname{div}[(v\cdot\nabla)w+(w\cdot\nabla)v]
                 =2\operatorname{div}h.
\]
The complete gradient-projection identity is therefore
\[
 E_{\rm press}=-4\langle\mathbb P_{\rm grad}(Qw),h\rangle,\qquad
 |E_{\rm press}|\le\frac4m
       \|r\,\operatorname{div}(Qw)\|_2\,\|h\|_2.
\]
The angular energy factor is \(4/m\), which equals one at \(m=4\).

The unit swirl has \(V=rC_0(r)q_v(z)\). As \(w_z=0\),
\[
 h=-\frac Vr w_\theta e_r+V_r w_r e_\theta .
\]
On the new support, \(C_0(r)=\chi(\xi)\),
\(0\le\chi\le1\), and \(-4<\chi'(\xi)\le0\), where
\(\xi=r^2/a^2\).
Thus
\[
 |V/r|\le1,\qquad
 |V_r|=|(\chi(\xi)+2\xi\chi'(\xi))q_v|
 \le\max(1,8\xi)<5,\qquad \xi\le(.24/.315)^2 .
\]
Consequently \(\|h\|_2\le5\|w\|_2\le5\sqrt{\pi R_0Z_0}\).
The new separated interval integrals for
\(n_d=\|r\,\operatorname{div}(Qw)\|_2\) retain the radial and axial
terms with their correct factors, exactly as in the audited Pass7
derivation. They give the recorded upper bound on
\(5n_d\sqrt{\pi R_0Z_0}\). This includes the full whole-space pressure.

Here the reversed phase makes \(E_{\rm loc}\) positive on the cone.
The script correctly retains
\[
 |E_{\rm loc}|\le
 135\cdot80\left(\int_{.04}^{.24}r^2\,dr\right)
             \frac{12/5}{(17/5)^7}<.023 .
\]
Combining it with the pressure bound gives the actual certified
\[
 |E|\le E_{\rm abs,+}<0.303613<1/3.
\]
The displayed interval encloses a positive upper-bound expression;
its lower endpoint is not a lower bound on \(|E|\).

## 4. Same-field central feedback, with its viscosity budget

The unchanged pump/core tensor estimates apply on this narrower
cone to any horizontal solenoidal seed, regardless of winding sign:
\[
 \mathcal L(M;w)\le-\frac{96}{5}C_{\rm lead}.
\]
The full viscous norm formula and the new \(R_1,R_0\) bounds yield
\[
 \nu d_w\le\frac6{1000}(5/17)^5
       [200000(12/5)+(392/5)(160/3)]
 =9078400/1419857<7.
\]
Hence \(D_\nu<7-(96/5)C_{\rm lead}\).

The exact retuned pressure polynomial is the same one used in Pass7,
with this new seed's coefficients. It gives
\[
 T_\lambda<-m_0+\lambda^2
   [(23199/1000)C_{\rm lead}+D_\nu+A_\lambda E].
\]
The bracket is at most the positive constant
\((3999/1000)(3/2)+7+1020/3\). Using \(\lambda^2\le1/16\) proves
\[
 T_\lambda<
 -\frac{290649}{8750}
 +\frac{(3999/1000)(3/2)+7+1020/3}{16}
 =-\frac{12493177}{1120000}.
\]
The lower bound on the seed amplitude, used for the torque test,
is compatible with this upper bound used for pressure.

The audit checker independently verifies the non-flat covariance
and torque algebra, exact interval endpoints, support/gradient
constants, retuning inequalities, energy margins, and common
central-pressure budget. No time integration is part of the
certificate. Persistence of the three gains, an inherited receiving
state, and repeated regeneration remain separate unsolved steps.
